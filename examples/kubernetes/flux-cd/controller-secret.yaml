# Option A: Plain Kubernetes Secret (for simple setups)
# Replace <base64-encoded-api-token> with: echo -n "your-api-token" | base64
apiVersion: v1
kind: Secret
metadata:
  name: forgejo-runner-controller-secret
  namespace: forgejo-runner-system
type: Opaque
data:
  token: <base64-encoded-api-token>
---
# Option B: ExternalSecret (for production setups using external-secrets-operator)
# Uncomment below and remove the plain Secret above if you use External Secrets.
#
# apiVersion: external-secrets.io/v1
# kind: ExternalSecret
# metadata:
#   name: forgejo-runner-controller
#   namespace: forgejo-runner-system
# spec:
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: my-secret-store          # Replace with your ClusterSecretStore name
#   target:
#     name: forgejo-runner-controller-secret
#     template:
#       data:
#         token: "{{ .FORGEJO_API_TOKEN }}"
#   dataFrom:
#     - extract:
#         key: forgejo-runner         # Replace with your secret store key
