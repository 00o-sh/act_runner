act-runner-controller has been installed.

Namespace: {{ .Release.Namespace }}
Service Account: {{ include "act-runner-controller.serviceAccountName" . }}
Forgejo URL: {{ .Values.forgejo.url }}
Reconcile Interval: {{ .Values.reconcileInterval | default 30 }}s
Scale-Down Delay: {{ .Values.scaleDownDelay | default 300 }}s

The controller will automatically scale runner Deployments/StatefulSets
labeled with 'app.kubernetes.io/managed-by: act-runner-controller'.

Next steps:
  1. Install one or more runner scale sets:

     helm install my-runners \
       oci://ghcr.io/00o-sh/act_runner/charts/act-runner-scale-set \
       -n act-runners --create-namespace \
       --set giteaConfigUrl={{ .Values.forgejo.url | default "https://forgejo.example.com" }} \
       --set giteaConfigSecret.token=<registration-token>

  2. Verify the controller is running:

     kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

  3. Check controller logs:

     kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} -f
